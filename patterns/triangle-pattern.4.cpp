#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    for (int j = 0; j <5; j++)
    {
        for (int i = j; i > 0; i--)
        {
            cout<<i;
        }
        cout << endl;
        
    }
    

    return 0;
}